#include "main.h"

/**
 * print_alphabet_x10 - print the alphabet 10 times.
 * 
 * Return: Always 0 (Success)
 */
void print_alphabet_x10(void)
{
	int i;
	while (i = 'a'; i <= 'z'; i++)
	{
		int j;
		while (j = 0; j <= 10; j++)
		{
			_putchar('\n');
		}
		_putcha(i);
	}
	return (0);
}
