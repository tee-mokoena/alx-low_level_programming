#include <stdio.h>

/**
 * main - Print alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char x = 'a';

	for (int x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	return (0);
}
